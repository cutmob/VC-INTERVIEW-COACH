# OpenAI API Configuration
OPENAI_API_KEY=sk-proj-...

# Stripe Configuration (OPTIONAL - leave empty to disable payments)
# If empty, the app will work in dev mode with unlimited free sessions
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PRICE_ID=

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Redis Configuration (optional, falls back to Netlify Blobs)
REDIS_URL=redis://localhost:6379

# Development Token Generation (optional)
# Leave empty for open dev mode (anyone can generate tokens locally)
# Set to a secret string to require authentication for token generation
# In production, this endpoint is automatically disabled unless this is set
DEV_TOKEN_SECRET=
